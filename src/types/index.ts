/**
 * Shapes more than one layer shares, behind the one `$types` import: what
 * crosses the IPC bridge, and the image model built from it.
 */
export type * from "./ipc";
export type * from "./image";
